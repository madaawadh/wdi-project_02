<br><br>
<h3 class="your_order"><%= @tag.tag %></h3>

<div class="cards">
<% @tag.recipes.each do |recipe| %>
 <div class="card">
        <div class="card-image">
        <%= image_tag recipe.image %>
        </div>
        <div class="card-action"> 
<%= link_to recipe.title, recipe %>
   </div>
      </div>
<%end%>
</div>